get:
  summary: Return all skills
  description: Returns all skills that the user has access to
  responses:
    '200':
      description: A list of skills
      content:
        application/json:
          schema:
            type: array
            items:
              type: string

